[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libGL", "libGLU", "glib"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python3.11 -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
